services:
  testing-database:
    image: nadeemlab/smprofiler-db-preloaded-1small
    container_name: smprofiler-db---testing-only-workflow
    ports:
      - target: 5432
        protocol: tcp
    networks:
      - isolated_temporary_test
    environment:
      POSTGRES_PASSWORD: postgres

networks:
  isolated_temporary_test:
    driver: bridge
