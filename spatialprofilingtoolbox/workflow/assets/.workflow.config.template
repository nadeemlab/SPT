[general]
db_config_file = path/to/db.config
executor = local
excluded_host = None
orchestrator = local
image = name/of/or/path/to/docker/or/singularity/image/if/not/local

[database visitor]
study_name = name_of_study

[tabular import]
input_path = path/to/input/directory

[cg-gnn]
graph_config_file = path/to/graph.config
cuda = true
upload_importances = false
